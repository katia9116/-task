Звіт до лабораторної роботи

Тема: Програмування циклічних алгоритмів
Варіант: 4
Мета роботи:Формування теоретичної бази знань циклічних алгоритмів.
Розвиток умінь і навичок ефективного використання операторів циклу для розробки програмних додатків.


3.1 Види циклів у C++:
for – цикл з параметром; використовується, коли відома кількість повторень.
while – цикл з передумовою; виконується, поки умова істинна.
do...while – цикл з післяумовою; виконується хоча б один раз, потім перевіряється умова.

3.2 Різниця між while та do...while:
while – перевіряє умову на початку; тіло циклу може не виконатися жодного разу.
do...while – перевіряє умову після виконання тіла циклу; тіло циклу виконується хоча б один раз.

3.3 Як записується цикл for:
Синтаксис: for (ініціалізація; умова; крок) { тіло циклу }
Використовується для циклів із заздалегідь відомою кількістю повторень.

ЗАВДАННЯ 1
#include <iostream>
#include <cmath>   // для sin() і fabs()

using namespace std;

int main() {
    double x, y;

    cout << "Обчислення значень функції y = sin^5(x) + |5x - 1.5|" << endl;

    for (int i = 1; i <= 5; i++) {
        cout << "Введіть x" << i << ": ";
        cin >> x;

        y = pow(sin(x), 5) + fabs(5 * x - 1.5);

        cout << "Для x = " << x << ", y = " << y << endl;
    }

    return 0;
}


ЗАВДАННЯ 2 

#include <iostream>
using namespace std;

int main() {
    int sum = 0;
    for (int i = 1; i <= 25; i++) {
        sum += i * i;
    }
    cout << "Сума квадратів чисел від 1 до 25 = " << sum << endl;
    return 0;
}

ЗАВДАННЯ 3
#include <iostream>
using namespace std;

int main() {
    double a, b, h;
    cout << "Введіть a, b, h: ";
    cin >> a >> b >> h;

    double x = a;
    cout << "-------------------\n";
    cout << ": X : Y :\n";
    cout << "-------------------\n";
    while (x <= b) {
        double y = x + x*x;
        cout << ": " << x << " : " << y << " :\n";
        x += h;
    }
    cout << "-------------------\n";
    return 0;
}

ЗАВДАННЯ 4
#include <iostream>
using namespace std;

int main() {
    int N;
    cout << "Введіть число N: ";
    cin >> N;

    int product = 1;
    int temp = N;
    int digit;

    if (temp == 0) product = 0;

    do {
        digit = temp % 10;
        if (digit % 2 != 0) product *= digit;
        temp /= 10;
    } while (temp > 0);

    cout << "Добуток непарних цифр числа = " << product << endl;
    return 0;
}
